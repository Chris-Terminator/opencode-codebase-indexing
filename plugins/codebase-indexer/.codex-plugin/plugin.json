{
  "name": "codebase-indexer",
  "version": "0.1.0",
  "description": "Reads and watches enrolled codebases, sends code chunks to a configured embedding provider, writes local cache data, and creates or deletes project-scoped Qdrant collections.",
  "author": {
    "name": "Chris-Terminator"
  },
  "skills": "./skills/",
  "mcpServers": "./.mcp.json",
  "interface": {
    "displayName": "Codebase Indexer",
    "shortDescription": "Qdrant-backed semantic code search.",
    "longDescription": "Reads and watches enrolled projects, sends supported code chunks to the configured embedding provider, stores them in Qdrant, writes a local synchronization cache, and exposes semantic_search through MCP.",
    "developerName": "Chris-Terminator",
    "category": "Productivity",
    "capabilities": [
      "Read",
      "Write"
    ],
    "defaultPrompt": [
      "Search this codebase semantically.",
      "Check the code index status."
    ]
  }
}
