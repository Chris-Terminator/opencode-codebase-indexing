# Third-Party Notices

The indexing engine under `src/indexing`, `src/tree-sitter`, `src/file`, and `src/util` is adapted from KiloCode's `@kilocode/kilo-indexing` package under the MIT License. See `LICENSE.kilocode`.
